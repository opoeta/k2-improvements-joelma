# Required for KAMP's adaptive purge — KAMP reads the print's polygon
# coordinates from the [exclude_object] objects list at LINE_PURGE time.
# Creality Print emits EXCLUDE_OBJECT_DEFINE blocks for every object on the
# plate, so this is enough — no slicer-side change needed.

[exclude_object]
